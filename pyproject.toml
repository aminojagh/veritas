[project]
name = "veritas"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "dlt>=1.29.1",
    "duckdb>=1.5.5",
    "fastembed>=0.8.0",
    "minsearch>=0.2.0",
    "pyyaml>=6.0.3",
    "sqlglot>=30.15.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
